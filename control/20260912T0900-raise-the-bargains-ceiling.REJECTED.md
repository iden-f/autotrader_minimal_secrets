# This change was not applied

Refused, and nothing was changed by these:
- `set-rule`: maximum_mileage is not a rule I can change from here. I can change: max_distance_km, max_mileage_km, max_price, max_year, min_price, min_year, near, price_drop_min_abs, price_drop_min_pct, require_price.

Nothing was written. Edit the issue and reopen it, or open a new one from the dashboard.

Nothing was changed - not even partly. Edit `control/20260912T0900-raise-the-bargains-ceiling.json` and
commit again; I read it on every push.
